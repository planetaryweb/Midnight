{{- range $.Members -}}
    {{- if eq .Name ($.Name) -}}
        {{- $.Scratch.Set "found" true -}}
        {{- $.Scratch.Set "member" . -}}
    {{- end -}}
{{- end -}}
{{- if not ($.Scratch.Get "found") -}}
    {{- errorf "Could not find member with name %s" $.Member -}}
{{- end -}}

